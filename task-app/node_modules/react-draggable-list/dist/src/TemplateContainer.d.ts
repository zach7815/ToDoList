import * as React from 'react';
interface Props<I, C, T> {
    item: I;
    template: new (props: any, context?: any) => T;
    itemSelected: number;
    anySelected: number;
    dragHandleProps: object;
    commonProps: C;
}
export default class TemplateContainer<I, C, T extends React.Component<any, any>> extends React.Component<Props<I, C, T>> {
    private _template;
    private readonly _templateSetter;
    shouldComponentUpdate(nextProps: Props<I, C, T>): boolean;
    getTemplate(): T;
    render(): JSX.Element;
}
export {};
